# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。只放交接必需資訊，一頁內讀完。

## ⏯️ 目前做到哪

**階段一（內容體系）與階段二主體（衛教單＋網站）皆已完成**，專案進入「實機驗證與微調」階段。

- **內容**：S1–S5 五階段冊＋M 更年期專項冊＋兩份 ref 共用清單，**八份全部審定**，`content/` 已無任何【待審】標記
- **衛教單**：**六張 A4 全部完成並上站**（S1–S5＋M），皆套用 F 能量斜紋底圖與五關旅程路徑
- **網站**：GitHub Pages 上線 https://philia81301-commits.github.io/obesity-education-clinic/ ，已掛進個人入口網站（卡片＋QR，門診衛教區）
- **數據**：50 歲分組專項分析完成並寫入 M 冊，每月隨月度分析更新

## 🚦 目前狀態

- **網站產生方式**：`docs/` 由 `tools/build-site.js` 從 `content/*.md` 自動產生（零相依 Node 腳本）。**改完 content 或新增衛教單，都要重跑 `node tools/build-site.js` 再 commit**，否則線上不會變
- **視覺定調 v2.1**（已入 agent 長期記憶）：飲食烈橘 #C2620A／運動勝利綠 #0A8A4D／藥物電光藍 #3563C9／成就暖金 #9A6B2A／紅 #C82D1B 只給紅旗；字體 LXGW WenKai TC＋Noto Sans TC；五階段＝第 1–5 關、停滯期＝魔王關；衛教單頁尾不放回診日
- **底圖已定案**：F 能量斜紋＋旅程路徑點（純程式 CSS/SVG；早期 AI 生圖 8 張已否決刪除）
- **網站字級（2026-08-25 依醫師反映調整）**：導覽列站名 21px 楷體、內文 17px／行高 1.78，其餘元素等比放大
- **M 冊 50 歲數據守則**：月度更新只換數字；若某月結論反轉（50+ 顯著較差），**不得自動改寫文字，先回報醫師**

## ➡️ 下一步

1. **衛教單診間印表機實機實測**（彩色＋黑白各一）——底色明度已拉開為灰階 243/231/218，螢幕模擬已驗證可區分、**實機未測**；列印務必勾選「背景圖形」，否則色塊與底紋不出現
2. **手機掃 QR 的實際閱讀體驗實測**（已做響應式，真機未看過）；另「回到頂端」按鈕的真實捲動行為也待真機確認（測試環境不派發 scroll 事件，僅以手動派發驗證過邏輯）
3. 遊戲化用語尺度（「魔王關」「戰績」病人接受度）待醫師最終拍板
4. 待醫師決定：內容頁的「醫師備註／出處」是否做成可摺疊——做了 M 冊可從 21 螢幕縮到約 8，但會改變本站「給病人看／給醫護看」的定位
5. 可選：向台灣更年期醫學會索取《2025 台灣更年期婦女健康管理及藥物治療建議》全文，補強 M 冊出處
6. 8 月底個案檔上傳後跑月度分析（第四步之四：更新 M 冊 50 歲分組數字；**11 歲個案改另列**）

## 🧩 新增衛教單時照做

- 底稿複製 `design/衛教單-S3停滯期-A4.html`（模組最多、間距最緊）
- 路徑亮點依關卡位移，已過的關轉 `opacity .45`
- 每張挑一個重點模組做成 `.core` 專區（S3 綠框核心菜單／S4 金框警戒線量尺／S5 金框「回來就好」／M 金框情緒壓力三招）
- 頁尾固定紅旗；完成後**量測頁尾底部 ≤1081px** 才算安全（`.sheet` 有 `overflow:hidden`，光看高度會被騙）
- 做完**加進 `tools/build-site.js` 的 `SHEETS` 陣列**再跑 build，否則不會上站

## ⚠️ 注意事項

- **中文內容檔只用 Edit 工具改，不要用 PowerShell `-replace`**（曾造成整份編碼毀損，靠 git 還原）
- commit 用明確路徑，**不要 `git add .`**（多 session 平行工作，會收進別人的檔案）
- 兩台電腦都放 `C:\projects\obesity-education-clinic\`，開工前 `git pull`、收工後 `git push`；不要放進 OneDrive／雲端硬碟
- 計畫書 docx（270 份問卷）是院內營運資料，留在 OneDrive，不 commit 進 repo；引用只取比例數字
- 衛教數值需標出處；病人用語控制在國中閱讀程度，生化機轉放醫師講解層
- **月度分析【醫師已定案 2026-08-25】**：<50 歲女性組那名 **11 歲個案（兒少）自下次月報起另列**，不併入 <50 歲成人女性組——`subgroup_female_age.js` 需加年齡下限（如 ≥18）與兒少另列邏輯，M 冊數字更新時一併採用新分組
- GitHub Pages 有瀏覽器快取，改版後看不到新版是正常的，Ctrl+Shift+R 強制重整

## 🕐 最後更新

- 時間：2026-08-25（網站上線＋六張衛教單完成＋閱讀體驗調校）
- 更新者：Claude Code（Fable 5）@ DESKTOP-LVSV9Q5
- Git push：✅ 已推（b6dfd75）
- L3 Obsidian：✅ 已寫 `2ndBrain/obesity-education-clinic/專案工作流程.md`（完整脈絡、決策紀錄、文獻查證、踩坑筆記都在那）
