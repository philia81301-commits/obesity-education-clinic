# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。只放交接必需資訊，一頁內讀完。

## ⏯️ 目前做到哪

**階段三（實機驗證）開跑**：2026-08-25 診間首輪彩色實印，抓到兩處裁字並當日修復上線——

1. **紙緣壓字**：紅旗警語原本距紙緣僅 11mm，印表機驅動下偏 4–5mm 後壓線被裁 → 六張 `.sheet` 底部 padding 11→16mm（墨水底線 286→281mm），S3 行距 1.56→1.5、區塊間距 2.8→2.5mm 擠出空間維持單頁（commit dc6b5bc）
2. **SVG 裁字頭**：「你在這裡」標籤字頂超出進度條 SVG 的 viewBox 上緣被裁（S1 僥倖沒中，S2–S5＋M 全中）→ `.path svg` 開 `overflow: visible`，字升進頁首分隔線下方 4mm 空白帶，版面高度不動（commit 5e5b930）

兩輪皆已重跑 build、push、線上逐張驗證生效。**修復後複印，醫師回報「測試正常」——衛教單列印驗證結案。**

## 🚦 目前狀態

- **網站產生方式**：`docs/` 由 `tools/build-site.js` 從 `content/*.md` 自動產生（零相依 Node 腳本）。**改完 content 或 design 衛教單，都要重跑 `node tools/build-site.js` 再 commit**，否則線上不會變
- **視覺定調 v2.1**（已入 agent 長期記憶）：飲食烈橘 #C2620A／運動勝利綠 #0A8A4D／藥物電光藍 #3563C9／成就暖金 #9A6B2A／紅 #C82D1B 只給紅旗；字體 LXGW WenKai TC＋Noto Sans TC；五階段＝第 1–5 關、停滯期＝魔王關；衛教單頁尾不放回診日
- **底圖已定案**：F 能量斜紋＋旅程路徑點（純程式 CSS/SVG）
- **網站字級（2026-08-25 依醫師反映調整）**：導覽列站名 21px 楷體、內文 17px／行高 1.78
- **M 冊 50 歲數據守則**：月度更新只換數字；若某月結論反轉（50+ 顯著較差），**不得自動改寫文字，先回報醫師**

## ➡️ 下一步

1. ~~列印實測收尾~~ **✅ 已完成（2026-08-25 醫師回報「測試正常」）**：兩處裁字修復後複印確認正常，黑白輪一併確認。**衛教單列印驗證結案**；日後新增衛教單仍須遵守下方版型慣例（安全線 1062px、勾「背景圖形」）
2. **手機掃 QR 真機閱讀體驗**（已做響應式，真機未看過）；「回到頂端」按鈕真實捲動行為也待真機確認
3. 遊戲化用語尺度（「魔王關」「戰績」病人接受度）待醫師最終拍板
4. 待醫師決定：內容頁「醫師備註／出處」是否做成可摺疊（會改變「給病人看／給醫護看」定位）
5. 可選：向台灣更年期醫學會索取《2025 台灣更年期婦女健康管理及藥物治療建議》全文，補強 M 冊出處
6. 8 月底個案檔上傳後跑月度分析（更新 M 冊 50 歲分組數字；**11 歲個案改另列**）

## 🧩 新增衛教單時照做

- 底稿複製 `design/衛教單-S3停滯期-A4.html`（模組最多、間距最緊、已含列印修正）
- 路徑亮點依關卡位移，已過的關轉 `opacity .45`；標籤字若在 SVG 頂端，記得 `.path svg` 要有 `overflow: visible`（否則字頭被 viewBox 裁掉）
- 每張挑一個重點模組做成 `.core` 專區（S3 綠框核心菜單／S4 金框警戒線量尺／S5 金框「回來就好」／M 金框情緒壓力三招）
- 頁尾固定紅旗；完成後**量測頁尾底部 ≤1062px（＝墨水底線 281mm）**才算安全——⚠️ 舊安全線 1081px 已作廢（那是會被印表機裁掉的位置）；`.sheet` 有 `overflow:hidden`，光看高度會被騙
- 做完**加進 `tools/build-site.js` 的 `SHEETS` 陣列**再跑 build，否則不會上站

## ⚠️ 注意事項

- **`.sheet` 底部 padding 16mm 不要改回 11mm**：印表機驅動會把內容下偏 4–5mm，貼紙緣的字會被裁——2026-08-25 紅旗警語被切半的事故就是這樣來的（CSS 內有註解）
- **SVG 內的文字標籤注意 viewBox 裁切**：超出 viewBox 的部分預設會被切掉，螢幕與列印都一樣；`.path svg` 已開 `overflow: visible` 解決
- **中文內容檔只用 Edit 工具改，不要用 PowerShell `-replace`**（曾造成整份編碼毀損，靠 git 還原）
- commit 用明確路徑，**不要 `git add .`**（多 session 平行工作，會收進別人的檔案）
- 兩台電腦都放 `C:\projects\obesity-education-clinic\`，開工前 `git pull`、收工後 `git push`；不要放進 OneDrive／雲端硬碟
- 計畫書 docx（270 份問卷）是院內營運資料，留在 OneDrive，不 commit 進 repo；引用只取比例數字
- 衛教數值需標出處；病人用語控制在國中閱讀程度，生化機轉放醫師講解層
- GitHub Pages 有瀏覽器快取，改版後看不到新版是正常的，Ctrl+Shift+R 強制重整

## 🕐 最後更新

- 時間：2026-08-25（列印實測**結案**：兩處裁字修復後複印確認正常）
- 更新者：Claude Code（Fable 5）@ DESKTOP-LVSV9Q5（前次為 Opus 5 @ X108521 診間電腦）
- Git push：✅ 已推（9ed2c80）
- L3 Obsidian：⚠️ 本機 2ndBrain 無 `obesity-education-clinic/` 資料夾（L3 筆記在 DESKTOP-LVSV9Q5 建的），本次未更新——回主力機收工時把這兩個列印坑補進踩坑筆記
