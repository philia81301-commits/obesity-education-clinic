# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。只放交接必需資訊，一頁內讀完。

## ⏯️ 目前做到哪

**階段一到三全部完成，專案進入可用狀態。** 內容體系八份審定、六張 A4 衛教單實印驗證通過、網站上線並分流病人／醫護視角。

2026-08-25 本日完成的三件事：

1. **列印實測結案**——首輪實印抓到兩處裁字並修復：紙緣壓到紅旗警語（底部 padding 11→16mm，墨水底線 286→281mm）、SVG viewBox 裁掉「你在這裡」字頭（`.path svg` 開 `overflow: visible`）。修復後複印確認正常。
2. **手機掃 QR 真機體驗、遊戲化用語（「魔王關」「戰績」）** 皆經醫師確認可用。
3. **內容頁「醫師備註／出處」可摺疊**（醫師定案「要做，預設收起」）——導覽列開關，選擇存 localStorage 跨頁記住。實測 M 冊頁高 9,580px（醫師版）→ 4,632px（病人版），**減少 52%**。

## 🚦 目前狀態

- **網站產生方式**：`docs/` 由 `tools/build-site.js` 從 `content/*.md` 自動產生（零相依 Node 腳本）。**改完 content 或 design 衛教單，都要重跑 `node tools/build-site.js` 再 commit**，否則線上不會變
- **摺疊機制**：模組內「醫師備註」「出處」條目 → `.docnote`；整段醫護專用區塊（醫師講解層／依據總表／組裝範例／出處）→ `.docsec`，目錄對應項連動隱藏。判定邏輯在 `isDocSection()` 與 `wrapDocSections()`
- **視覺定調 v2.1**：飲食烈橘 #C2620A／運動勝利綠 #0A8A4D／藥物電光藍 #3563C9／成就暖金 #9A6B2A／紅 #C82D1B 只給紅旗；字體 LXGW WenKai TC＋Noto Sans TC；五階段＝第 1–5 關、停滯期＝魔王關；衛教單頁尾不放回診日
- **網站字級**：導覽列站名 21px 楷體、內文 17px／行高 1.78
- **M 冊 50 歲數據守則**：月度更新只換數字；若某月結論反轉（50+ 顯著較差），**不得自動改寫文字，先回報醫師**

## ➡️ 下一步

專案主體已完成，剩下的都是可選或週期性事項：

1. **8 月底個案檔上傳後跑月度分析**——更新 M 冊 50 歲分組數字；**11 歲個案改另列**（`subgroup_female_age.js` 需加年齡下限與兒少另列邏輯）
2. 可選：向台灣更年期醫學會索取《2025 台灣更年期婦女健康管理及藥物治療建議》全文，補強 M 冊出處
3. 可選：`衛教圖示-少吃多動不夠的生化解釋` 的 **v3 圖檔仍含舊的「BMR 10–40%」數字**（筆記文字已勘誤），下次重做圖時出 v4

## 🧩 新增衛教單時照做

- 底稿複製 `design/衛教單-S3停滯期-A4.html`（模組最多、間距最緊、已含列印修正）
- 路徑亮點依關卡位移，已過的關轉 `opacity .45`；標籤字若在 SVG 頂端，`.path svg` 要有 `overflow: visible`
- 每張挑一個重點模組做成 `.core` 專區（S3 綠框核心菜單／S4 金框警戒線量尺／S5 金框「回來就好」／M 金框情緒壓力三招）
- 頁尾固定紅旗；完成後**量測頁尾底部 ≤1062px（＝墨水底線 281mm）**才算安全——⚠️ 舊安全線 1081px 已作廢（會被印表機裁掉）；`.sheet` 有 `overflow:hidden`，光看高度會被騙
- 做完**加進 `tools/build-site.js` 的 `SHEETS` 陣列**再跑 build，否則不會上站

## ⚠️ 注意事項

- **`.sheet` 底部 padding 16mm 不要改回 11mm**：印表機驅動會把內容下偏 4–5mm，貼紙緣的字會被裁（2026-08-25 紅旗警語被切半的事故；CSS 內有註解）
- **SVG 內的文字標籤注意 viewBox 裁切**：超出部分預設會被切掉，螢幕與列印都一樣
- **中文內容檔只用 Edit 工具改，不要用 PowerShell `-replace`**（曾造成整份編碼毀損，靠 git 還原）
- commit 用明確路徑，**不要 `git add .`**（多 session 平行工作）
- 兩台電腦都放 `C:\projects\obesity-education-clinic\`，開工前 `git pull`、收工後 `git push`；不要放進 OneDrive／雲端硬碟
- 計畫書 docx（270 份問卷）是院內營運資料，留在 OneDrive，不 commit 進 repo；引用只取比例數字
- 衛教數值需標出處；病人用語控制在國中閱讀程度，生化機轉放醫師講解層
- GitHub Pages 有瀏覽器快取，改版後看不到新版是正常的，Ctrl+Shift+R 強制重整

## 🕐 最後更新

- 時間：2026-08-25（階段三結案：列印實測、QR、遊戲化用語、醫師備註摺疊）
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5
- Git push：✅ 已推（0f29be1）
- L3 Obsidian：本次已更新 `2ndBrain/obesity-education-clinic/專案工作流程.md`
